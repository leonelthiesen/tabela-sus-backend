import { SigtapProcedimentoIncremento } from './sigtap-procedimento-incremento';

describe('SigtapProcedimentoIncremento', () => {
  it('should be defined', () => {
    expect(new SigtapProcedimentoIncremento()).toBeDefined();
  });
});
